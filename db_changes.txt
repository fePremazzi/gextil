- renomear nomes dos colunas, tipo de "nome_usuario" para apenas "nome"
- adicionar nova tabela "tbRole" tabela de permissoes e adicionar as roles "ADMIN" e "COMUM"
- adicionar nova tabela "tbCargos" e adicionar uns dois cargos que voce acha que deve ter
- mudar tabela "tbUsuario":
CREATE TABLE tbUsuario
(
	id int IDENTITY(1,1) PRIMARY KEY,
	nome varchar(30) NOT NULL,
	id_cargo int NOT NULL,
	username varchar(30) NOT NULL,
	senha varchar(max) NOT NULL,
    	id_role int NOT NULL,
	foto_caminho varchar(max) NOT NULL
);

Tamanho da senha precisa se rgrande pois vou tentar encriptografar essa merda